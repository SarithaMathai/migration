import _ from 'lodash'

export const getHumanIdsFromResourcesList = (resources) => (!!resources && _.isArray(resources) && resources.length > 0) ? resources.map(it => it.humanId).filter(it => !!it) : []
