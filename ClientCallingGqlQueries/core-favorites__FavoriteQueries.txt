import { gql } from '@apollo/client'

export const GET_FAVORITE_TYPES_FOR_CURRENT_USER = gql`
  query getFavoriteTypesForCurrentUser {
    getFavoriteTypesForCurrentUser
  }
`

export const ADD_FAVORITE = gql`
  mutation addFavorite($resource: SPARK_FavoriteResourceInput!) {
    addFavorite(resource: $resource) {
      id
      resource {
        id
      }
    }
  }
`

export const DELETE_FAVORITE = gql`
  mutation deleteFavorite($type: String!, $id: ID!) {
    deleteFavorite(type: $type, id: $id)
  }
`

export const IS_ENTITY_FAVORITE = gql`
  query isEntityFavorite($resource: SPARK_FavoriteResourceInput!) {
    isFavorite(resource: $resource) {
      resource {
        id
      }
    }
  }
`
